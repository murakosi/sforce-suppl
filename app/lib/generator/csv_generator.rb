module Generator
	class CsvGenerator
		
		def initialize(encoding, line_feed, force_quotes)
			@encoding = encoding
			@line_feed = line_feed
			@force_quotes = force_quotes
		end

		def generate(params)
		end
	end
end